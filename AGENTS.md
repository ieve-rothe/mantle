Environment Requirements

This project uses the Crystal Programming Language.

    Compiler: crystal

    Package Manager: shards

    Test Runner: crystal spec
    If the environment is missing the compiler, run: curl -fsSL https://crystal-lang.org/install.sh | sudo bash.